#include <stdio.h>

int y = (1);

int main()
{
    int x = 1;
    switch (x)
    {
    case 1:
        put(x);
        break;

    default:
        break;
    }
    if(x)
    {
        put(x);
    }
}